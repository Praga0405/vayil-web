# Keep backend/ in the Vercel build context — api/[...all].ts imports
# backend/src/index.ts as a serverless function. Exclude only the
# files that aren't needed at runtime.
backend/Dockerfile
backend/scripts/smoke-*.ts
