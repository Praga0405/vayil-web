'use client'
import VendorStudioLayout from '@/components/shared/VendorStudioLayout'

export default function Layout({ children }: { children: React.ReactNode }) {
  return <VendorStudioLayout>{children}</VendorStudioLayout>
}
